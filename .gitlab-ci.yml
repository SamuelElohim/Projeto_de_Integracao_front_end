image: rvancea/maven-chrome-jdk8:latest

build:
  stage: build
  script:
    - mvn package
  artifacts:
    name: "Executavel-Projeto-de-Integracao"
    paths:
      - target\jfx\native\*.exe

deploy:
  stage: deploy
  dependencies: 
    - build
  script:
    - copy target\jfx\native\*.exe "\\SAMUEL-CONRADO\Deploy\Develop\Projeto_de_Integracao-1.0.exe" 
